extends layout

block content
  .w3-container
    if locals.user
      h4 Bem Vindo, Mateus!
      ul
        each val in connectedResult
          li= val.name
    else
      h4 Você não está logado! Por Favor #[a(href="/login") Entre ] para Continuar.
